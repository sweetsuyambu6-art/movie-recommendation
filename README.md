# movie-recommendation
The system uses Natural Language Processing (NLP) techniques to process movie storylines.Each movie's storyline is converted into a numerical representation using TF-IDF Vectorization (or CountVectorizer).Based on the similarity scores, it returns the top five most similar movies as recommendations.
